package com.example.doctor_profile.navigation

enum class DoctorProfileAccessType {
    TOKEN_ACCESS,
    VIEWER_ACCESS,
    ADMIN_ACCESS,
    // ADMIN_ACCESS // (Example) Administrator is accessing the profile.
}