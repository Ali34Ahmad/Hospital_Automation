package com.example.employee_profile.navigation

enum class EmployeeProfileAccessType {
    TOKEN_ACCESS,
    EMPLOYEE_ID_ACCESS,
    ADMIN_ACCESS,
}
    // ADMIN_ACCESS // (Example) Administrator is accessing the profile.
